# React Native needs a flat node_modules: Metro resolution, community-CLI
# autolinking, and CocoaPods all walk node_modules expecting hoisted packages.
# pnpm's default symlinked/isolated layout breaks those, so use the hoisted linker.
node-linker=hoisted
